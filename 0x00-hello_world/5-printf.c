#include <stdio.h>
/**
 * main - Print a string
 * Description: use Printf
 * Return: 0 if success
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
