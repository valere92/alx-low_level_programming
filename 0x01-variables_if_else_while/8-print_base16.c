#include <stdio.h>
/**
 * main - inter point
 * Description: lower case in hexadecimal
 * Return: 0 as success
 */
int main(void)
{
int i;
int ch;
for (i = 48; i <= 57; i++)
putchar(i);
for (ch = 97; ch <= 102; ch++)
putchar(ch);
putchar(10);
return (0);
}
